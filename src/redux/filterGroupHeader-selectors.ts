export const getFilterId = (state: any) => {
    //return state.filterGroupHeader.filterHeaders.id
}

export const getFilterTitle = (state: any) => {
    //return state.filterGroupHeader.filterHeaders.title
}

export const getFilterVisibility = (state: any) => {
   // return state.filterGroupHeader.filterHeaders.filterVisibility
}